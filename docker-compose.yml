version: '3.8'
services:
  db:
    image: postgres:9.6
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=biblioteca
      - POSTGRES_PASSWORD=12345
      - MAX_CONNECTIONS=300